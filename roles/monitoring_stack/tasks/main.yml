---
- name: Ensure monitoring directories exist
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop: "{{ monitoring_required_directories }}"

- name: Deploy Prometheus config
  ansible.builtin.template:
    src: prometheus.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/prometheus/prometheus.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Prometheus alerts config
  ansible.builtin.template:
    src: alerts.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/prometheus/alerts.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Alertmanager config
  ansible.builtin.template:
    src: alertmanager.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/alertmanager/alertmanager.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Blackbox config
  ansible.builtin.template:
    src: blackbox.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/blackbox/blackbox.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Loki config
  ansible.builtin.template:
    src: loki-config.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/loki/loki-config.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Grafana environment file
  ansible.builtin.template:
    src: grafana.env.j2
    dest: "{{ monitoring_stack_install_dir }}/grafana/grafana.env"
    owner: root
    group: root
    mode: "0600"
  notify: Restart monitoring stack

- name: Deploy Grafana datasources provisioning
  ansible.builtin.template:
    src: grafana/provisioning/datasources/datasources.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/grafana/provisioning/datasources/datasources.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Deploy Grafana dashboards provisioning
  ansible.builtin.template:
    src: grafana/provisioning/dashboards/dashboards.yml.j2
    dest: "{{ monitoring_stack_install_dir }}/grafana/provisioning/dashboards/dashboards.yml"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Include Proxmox exporter tasks
  ansible.builtin.import_tasks: pve_exporter.yml
  when: monitoring_pve_exporter_enabled | bool

- name: Include Grafana dashboards tasks
  ansible.builtin.import_tasks: dashboards.yml

- name: Deploy Docker Compose file
  ansible.builtin.template:
    src: compose.yml.j2
    dest: "{{ monitoring_compose_file }}"
    owner: root
    group: root
    mode: "0644"
  notify: Restart monitoring stack

- name: Start monitoring stack
  ansible.builtin.command:
    cmd: docker-compose -f {{ monitoring_compose_file }} up -d --force-recreate
  changed_when: false
